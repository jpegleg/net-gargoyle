#!/bin/bash
# Read from ss (netstat) every X seconds, depending on your disk space.
while true; do
  ss -an | grep -v State | while read line; do
    echo " $line" >> /var/log/net.log &&
    sleep 0.1;
  done;
done
